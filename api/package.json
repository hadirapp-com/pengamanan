{
  "name": "pokayoke-api",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "dev:pengamanan": "bun run --hot src/index-pengamanan.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "build:pengamanan": "bun build src/index-pengamanan.ts --outdir dist --target node",
    "start": "bun run src/index.ts",
    "start:pengamanan": "bun run src/index-pengamanan.ts",
    "health-check": "curl -f http://localhost:3000/ || exit 1",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "bun run src/seed.ts",
    "db:seed:pengamanan": "bun run src/seed-pengamanan.ts",
    "openapi": "bun run src/openapi-generator.ts",
    "docker:dev": "docker-compose -f docker-compose.dev.yml up -d",
    "docker:dev:build": "docker-compose -f docker-compose.dev.yml up -d --build",
    "docker:dev:logs": "docker-compose -f docker-compose.dev.yml logs -f",
    "docker:dev:stop": "docker-compose -f docker-compose.dev.yml down",
    "docker:dev:reset": "docker-compose -f docker-compose.dev.yml down -v && docker-compose -f docker-compose.dev.yml up -d"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.2.1",
    "change-case": "^5.4.4",
    "drizzle-orm": "^0.44.4",
    "exceljs": "^4.4.0",
    "hono": "^4.8.5",
    "jose": "^5.9.6",
    "jspdf": "^2.5.2",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.13",
    "postgres": "^3.4.7",
    "qrcode": "^1.5.4",
    "xlsx": "^0.18.5",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/nodemailer": "^7.0.9",
    "drizzle-kit": "^0.31.4",
    "typescript": "^5.9.2",
    "wrangler": "^3.0.0"
  }
}
